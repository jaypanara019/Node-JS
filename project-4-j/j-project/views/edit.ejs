<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Patient</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h2 class="text-center">Update Patient</h2>
        <form action="/updateData/<%= patient._id %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label>Patient Name:</label>
                <input type="text" name="patient_name" value="<%= patient.patient_name %>" required class="form-control">
            </div>
            <div class="mb-3">
                <label>Age:</label>
                <input type="number" name="patient_age" value="<%= patient.patient_age %>" required class="form-control">
            </div>
            <div class="mb-3">
                <label>Gender:</label>
                <select name="patient_gender" class="form-control">
                    <option value="Male" <%= patient.patient_gender === 'Male' ? 'selected' : '' %>>Male</option>
                    <option value="Female" <%= patient.patient_gender === 'Female' ? 'selected' : '' %>>Female</option>
                </select>
            </div>
            <div class="mb-3">
                <label>Disease:</label>
                <input type="text" name="disease" value="<%= patient.disease %>" required class="form-control">
            </div>
            <div class="mb-3">
                <label>Upload New Photo:</label>
                <input type="file" name="patient_photo" class="form-control">
                <br>
                <img src="<%= patient.patient_photo %>" alt="Patient Photo" width="100">
            </div>
            <button type="submit" class="btn btn-primary">Update</button>
        </form>
        
    </div>
</body>
</html>
